# 定时任务通用推送架构改造方案

> **文档版本**: v2.0  
> **创建日期**: 2026-06-11  
> **适用范围**: 所有第三方Bot（微信、飞书、钉钉、企业微信等）

---

## 📋 目录

1. [需求分析](#需求分析)
2. [当前问题诊断](#当前问题诊断)
3. [通用架构设计](#通用架构设计)
4. [核心修复方案](#核心修复方案)
5. [推送集成指南](#推送集成指南)
6. [实施路线图](#实施路线图)

---

## 一、需求分析

### 1.1 核心需求

✅ **让定时任务流程完整跑通**（当前存在严重bug）
- 任务能正确触发（不重复、不遗漏）
- 任务执行结果能被记录
- 失败任务有重试机制

✅ **支持多种第三方机器人推送**
- 微信Bot（已部分实现）
- 飞书Bot（`frontends/feishuapp.py`）
- 钉钉Bot（`frontends/dingtalkapp.py`）
- 企业微信Bot（`frontends/wecomapp.py`）
- Telegram、Discord等（未来可扩展）

✅ **通用推送接口**
- 各Bot使用统一的订阅模式
- 调度器无需感知具体推送渠道
- 支持同时推送到多个渠道

### 1.2 非功能需求

- **可靠性**：调度器崩溃不影响Bot，Bot崩溃不影响调度器
- **可扩展性**：新增Bot只需实现订阅接口
- **可观测性**：完整的日志链路（触发 → 执行 → 推送）
- **易用性**：用户可通过GA对话创建任务

---

## 二、当前问题诊断

### 2.1 严重bug分析

#### ❌ 问题1：任务疯狂重复触发

**日志证据**：
```log
2026-06-11 07:42 INFO TRIGGER daily_news_summary (last_run=None)
2026-06-11 07:42 INFO TRIGGER daily_news_summary (last_run=None)  ← 30秒后再次触发
2026-06-11 07:43 INFO TRIGGER daily_news_summary (last_run=None)
...
[1小时内触发约90次]
```

**根因分析**：

```python
# scheduler.py:119-124 (已修改的代码)
retry_cooldown = timedelta(minutes=int(task.get('retry_cooldown_minutes', 60)))
last_attempt = _last_attempt.get(tid)  # ← 从内存字典读取
if last_attempt and (now - last_attempt) < retry_cooldown: continue

# 触发
_last_attempt[tid] = now  # ← 写入内存字典
```

**失效场景**：

| 场景 | 现象 | 原因 |
|------|------|------|
| **done目录为空** | 首次运行时疯狂重复 | `last_run=None`，无任何防重 |
| **scheduler.py热重载** | 重载后立即重复触发 | `_last_attempt` 字典被清空 |
| **进程重启** | 重启后立即重复 | 内存字典丢失 |
| **双调度器** | 两个实例同时触发 | 各自维护独立的内存字典 |

**问题链**：

```
调度器每120s轮询
    ↓
检查 done/ 目录（为空）
    ↓
检查 _last_attempt（也为空 或 刚热重载清空）
    ↓
触发任务 → agent.put_task()
    ↓
任务进入GA队列（需要20分钟执行）
    ↓
调度器继续轮询（120s后）
    ↓
done/ 目录仍为空（任务还在执行）
    ↓
再次触发 ← ❌ 重复！
```

---

#### ❌ 问题2：双调度器实例冲突

**冲突代码**：

```python
# wechatapp.py:426-446 (你新增的代码)
def start_scheduler(bot, target_uid=''):
    script = os.path.join(_ROOT_DIR, 'reflect', 'scheduler.py')
    def _loop():
        mod = _load_scheduler_module(script)  # ← 新的模块实例
        while True:
            task = mod.check()  # ← 独立运行
            if task:
                _run_scheduler_task(bot, task, ...)
```

**端口锁为什么失效**：

```python
# scheduler.py:6-9
try: _lock  # ← 检查变量是否存在
except NameError:  # ← 首次导入时执行
    _lock = _socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    _lock.bind(('127.0.0.1', 45762))
```

**问题**：
1. `importlib.util.module_from_spec()` 创建**新模块对象**
2. 每个Python进程有**独立的命名空间**
3. 第二个进程尝试bind时会抛`OSError`，但**代码未捕获异常**
4. 微信Bot在**线程内**加载模块，与agentmain进程的端口锁**互不影响**

---

#### ⚠️ 问题3：长时间阻塞

```python
# wechatapp.py:395-400
def _run_scheduler_task(bot, task, target_uid='', target_ctx=''):
    # ...
    try:
        while 'done' not in item:
            item = dq.get(timeout=1200)  # ← 阻塞20分钟
```

**影响**：
- 调度器线程被占用，无法处理其他任务
- 如果GA卡住，后续所有任务延迟
- 无法中途取消任务

---

### 2.2 架构问题

#### 当前架构（混乱）

```
用户手动创建 sche_tasks/*.json
    ↓
┌───────────────────────────────────────┐
│  调度器实例1：agentmain --reflect     │ ← launch.pyw --sched
│     ↓                                  │
│  agent.put_task() → GA执行             │
│     ↓                                  │
│  写报告到 done/                        │
└───────────────────────────────────────┘

┌───────────────────────────────────────┐
│  调度器实例2：微信Bot内线程            │ ← launch.pyw --wechat --sched
│     ↓                                  │
│  agent.put_task() → GA执行（重复！）   │
│     ↓                                  │
│  推送到微信                            │
└───────────────────────────────────────┘

问题：
❌ 两个调度器同时运行，重复触发
❌ 微信Bot与调度器耦合
❌ 其他Bot无法复用推送逻辑
```

---

## 三、通用架构设计

### 3.1 目标架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        定时任务通用推送架构                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  [用户/GA] ──创建──> sche_tasks/*.json                            │
│                            ↓                                      │
│  ┌──────────────────────────────────────────────────┐            │
│  │  🔧 调度器层（Scheduler Layer）                   │            │
│  │  agentmain --reflect reflect/scheduler.py        │            │
│  │  • 唯一实例（端口锁）                             │            │
│  │  • 持久化防重（.last_attempt.json）               │            │
│  │  • 触发任务执行                                   │            │
│  └──────────────────────────────────────────────────┘            │
│                 ↓                                                 │
│        agent.put_task() → GA执行                                  │
│                 ↓                                                 │
│        写报告 + 写元数据                                           │
│                 ↓                                                 │
│  ┌──────────────────────────────────────────────────┐            │
│  │  📢 通知层（Notification Broker）                 │            │
│  │  temp/scheduler_notifications/                   │            │
│  │  • 结果队列（JSONL格式）                          │            │
│  │  • 通用事件格式                                   │            │
│  │  • 多订阅者支持                                   │            │
│  └──────────────────────────────────────────────────┘            │
│                 ↓                                                 │
│        ┌────────┼────────┬────────┬────────┐                     │
│        ↓        ↓        ↓        ↓        ↓                     │
│     微信Bot  飞书Bot  钉钉Bot  企微Bot  其他Bot                   │
│     (订阅)   (订阅)   (订阅)   (订阅)   (订阅)                   │
│        ↓        ↓        ↓        ↓        ↓                     │
│     推送到   推送到   推送到   推送到   推送到                    │
│      微信     飞书     钉钉     企微    其他平台                  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 核心设计原则

#### 1. 单一调度器（Single Scheduler）

- ✅ **唯一实例**：通过端口锁确保只有一个调度器运行
- ✅ **独立进程**：`agentmain --reflect`，不依赖任何Bot
- ✅ **职责单一**：只负责触发任务，不负责推送

#### 2. 通用通知层（Notification Broker）

- ✅ **标准化事件格式**：所有Bot订阅相同的事件流
- ✅ **解耦**：调度器不感知有哪些订阅者
- ✅ **持久化**：基于文件（JSONL），支持离线消费

#### 3. 订阅者模式（Subscriber Pattern）

- ✅ **统一接口**：所有Bot实现相同的订阅逻辑
- ✅ **独立运行**：各Bot进程独立，互不影响
- ✅ **过滤能力**：Bot可选择订阅特定任务

---

### 3.3 通知事件格式

**文件**: `temp/scheduler_notifications/events.jsonl`

**格式**（每行一个JSON对象）：

```json
{
  "event_id": "uuid-v4",
  "timestamp": "2026-06-11T10:30:00.123456",
  "event_type": "task_triggered|task_completed|task_failed",
  "task_id": "daily_news_summary",
  "task_config": {
    "schedule": "07:30",
    "repeat": "daily"
  },
  "result": {
    "success": true,
    "report_path": "sche_tasks/done/2026-06-11_1030_daily_news_summary.md",
    "execution_time_seconds": 1234,
    "error": null
  },
  "metadata": {
    "trigger_time": "2026-06-11T07:30:00",
    "complete_time": "2026-06-11T08:00:00"
  }
}
```

**事件类型**：

| 事件类型 | 触发时机 | 包含字段 |
|---------|---------|---------|
| `task_triggered` | 任务开始执行 | `task_id`, `task_config` |
| `task_completed` | 任务成功完成 | `task_id`, `result.report_path` |
| `task_failed` | 任务执行失败 | `task_id`, `result.error` |

---

### 3.4 订阅者接口规范

**所有Bot必须实现**：

```python
class TaskNotificationSubscriber:
    """任务通知订阅者基类"""
    
    def __init__(self, bot_instance):
        self.bot = bot_instance
        self.event_file = os.path.join(_TEMP_DIR, 'scheduler_notifications/events.jsonl')
        self.offset_file = os.path.join(_TEMP_DIR, f'.{self.__class__.__name__}_offset')
        self._load_offset()
    
    def _load_offset(self):
        """加载上次读取位置"""
        try:
            with open(self.offset_file) as f:
                self.offset = int(f.read().strip())
        except:
            self.offset = 0
    
    def _save_offset(self):
        """保存读取位置"""
        with open(self.offset_file, 'w') as f:
            f.write(str(self.offset))
    
    def poll_events(self):
        """轮询新事件（非阻塞）"""
        if not os.path.exists(self.event_file):
            return []
        
        events = []
        with open(self.event_file, encoding='utf-8') as f:
            f.seek(self.offset)
            for line in f:
                try:
                    event = json.loads(line.strip())
                    if self.should_handle(event):
                        events.append(event)
                except json.JSONDecodeError:
                    pass
            self.offset = f.tell()
        
        self._save_offset()
        return events
    
    def should_handle(self, event):
        """过滤：是否处理此事件（子类可覆盖）"""
        # 默认只处理 task_completed
        return event.get('event_type') == 'task_completed'
    
    def handle_event(self, event):
        """处理事件（子类必须实现）"""
        raise NotImplementedError
    
    def start(self):
        """启动订阅（后台线程）"""
        def _loop():
            while True:
                events = self.poll_events()
                for event in events:
                    try:
                        self.handle_event(event)
                    except Exception as e:
                        print(f'[{self.__class__.__name__}] handle error: {e}')
                time.sleep(10)  # 每10秒检查一次
        
        threading.Thread(target=_loop, daemon=True).start()
```

---

## 四、核心修复方案

### 4.1 修复调度器（P0 - Critical）

#### 修改1：持久化 `_last_attempt`

**文件**: `reflect/scheduler.py`

```python
import os, json, time as _time, socket as _socket, logging
from datetime import datetime, timedelta

# ... 现有代码 ...

# 持久化路径
_ATTEMPT_FILE = os.path.join(_dir, '../sche_tasks/.last_attempt.json')
_NOTIFICATION_DIR = os.path.join(_dir, '../temp/scheduler_notifications')
_NOTIFICATION_FILE = os.path.join(_NOTIFICATION_DIR, 'events.jsonl')

def _load_attempts():
    """加载尝试记录"""
    try:
        with open(_ATTEMPT_FILE, encoding='utf-8') as f:
            data = json.load(f)
        return {k: datetime.fromisoformat(v) for k, v in data.items()}
    except (FileNotFoundError, json.JSONDecodeError, ValueError):
        return {}

def _save_attempt(tid, timestamp):
    """保存尝试记录（原子写入）"""
    attempts = _load_attempts()
    attempts[tid] = timestamp
    
    # 原子写入
    temp_file = _ATTEMPT_FILE + '.tmp'
    with open(temp_file, 'w', encoding='utf-8') as f:
        json.dump({k: v.isoformat() for k, v in attempts.items()}, 
                  f, ensure_ascii=False, indent=2)
    os.replace(temp_file, _ATTEMPT_FILE)
    
    _logger.info(f'PERSIST attempt {tid} at {timestamp.isoformat()}')

def _cleanup_old_attempts(max_age_days=7):
    """清理过期记录"""
    attempts = _load_attempts()
    cutoff = datetime.now() - timedelta(days=max_age_days)
    cleaned = {k: v for k, v in attempts.items() if v > cutoff}
    
    if len(cleaned) < len(attempts):
        temp_file = _ATTEMPT_FILE + '.tmp'
        with open(temp_file, 'w', encoding='utf-8') as f:
            json.dump({k: v.isoformat() for k, v in cleaned.items()},
                      f, ensure_ascii=False, indent=2)
        os.replace(temp_file, _ATTEMPT_FILE)
        _logger.info(f'CLEANUP {len(attempts) - len(cleaned)} old attempts')

def _publish_event(event_type, task_id, task_config, result=None, metadata=None):
    """发布事件到通知层"""
    os.makedirs(_NOTIFICATION_DIR, exist_ok=True)
    
    event = {
        'event_id': f'{int(_time.time() * 1000000)}_{task_id}',
        'timestamp': datetime.now().isoformat(),
        'event_type': event_type,
        'task_id': task_id,
        'task_config': task_config,
        'result': result or {},
        'metadata': metadata or {}
    }
    
    # 追加写入JSONL
    with open(_NOTIFICATION_FILE, 'a', encoding='utf-8') as f:
        f.write(json.dumps(event, ensure_ascii=False) + '\n')
    
    _logger.info(f'PUBLISH {event_type} for {task_id}')

def check():
    """调度器主逻辑（修复版）"""
    
    # L4 archive (保持原有逻辑)
    global _l4_t
    if _time.time() - _l4_t > 43200:
        _l4_t = _time.time()
        try:
            import sys
            sys.path.insert(0, os.path.join(_dir, '../memory/L4_raw_sessions'))
            from compress_session import batch_process
            raw_dir = os.path.join(_dir, '../temp/model_responses')
            r = batch_process(raw_dir, dry_run=False)
            print(f'[L4 cron] {r}')
        except Exception as e:
            _logger.error(f'L4 archive failed: {e}')
    
    # 清理过期尝试记录
    _cleanup_old_attempts()
    
    if not os.path.isdir(TASKS):
        return None
    
    now = datetime.now()
    os.makedirs(DONE, exist_ok=True)
    done_files = set(os.listdir(DONE))
    
    # 加载持久化的尝试记录
    attempt_cache = _load_attempts()
    
    for f in sorted(os.listdir(TASKS)):
        if not f.endswith('.json'):
            continue
        
        tid = f[:-5]
        try:
            with open(os.path.join(TASKS, f), encoding='utf-8') as fp:
                task = json.loads(fp.read())
        except Exception as e:
            _logger.error(f'JSON parse error for {f}: {e}')
            continue
        
        if not task.get('enabled', False):
            continue
        
        repeat = task.get('repeat', 'daily')
        sched = task.get('schedule', '00:00')
        try:
            h, m = map(int, sched.split(':'))
        except Exception as e:
            _logger.error(f'Invalid schedule format in {f}: {sched!r} ({e})')
            continue
        
        # weekday任务：周末跳过
        if repeat == 'weekday' and now.weekday() >= 5:
            continue
        
        # 还没到schedule时间就跳过
        if now.hour < h or (now.hour == h and now.minute < m):
            continue
        
        # 执行窗口检查
        max_delay = task.get('max_delay_hours', DEFAULT_MAX_DELAY)
        sched_minutes = h * 60 + m
        now_minutes = now.hour * 60 + now.minute
        if (now_minutes - sched_minutes) > max_delay * 60:
            _logger.info(f'SKIP {tid}: {now_minutes - sched_minutes}min past schedule, '
                         f'exceeds max_delay={max_delay}h')
            continue
        
        # 检查冷却（基于done文件）
        last = _last_run(tid, done_files)
        cooldown = _parse_cooldown(repeat)
        if last and (now - last) < cooldown:
            continue
        
        # 检查retry_cooldown（基于持久化记录）
        retry_cooldown = timedelta(minutes=int(task.get('retry_cooldown_minutes', 60)))
        last_attempt = attempt_cache.get(tid)
        if last_attempt and (now - last_attempt) < retry_cooldown:
            _logger.debug(f'SKIP {tid}: retry_cooldown not expired '
                          f'(last_attempt={last_attempt.isoformat()})')
            continue
        
        # 触发：先持久化，再返回
        trigger_time = now
        _save_attempt(tid, trigger_time)
        
        _logger.info(f'TRIGGER {tid} (repeat={repeat}, schedule={sched}, '
                     f'last_run={last}, last_attempt={last_attempt})')
        
        # 发布触发事件
        _publish_event('task_triggered', tid, task, metadata={
            'trigger_time': trigger_time.isoformat(),
            'last_run': last.isoformat() if last else None
        })
        
        ts = now.strftime('%Y-%m-%d_%H%M')
        rpt = os.path.join(DONE, f'{ts}_{tid}.md')
        prompt = task.get('prompt', '')
        
        return (f'[定时任务] {tid}\n'
                f'[报告路径] {rpt}\n\n'
                f'先读 scheduled_task_sop 了解执行流程，然后执行以下任务：\n\n'
                f'{prompt}\n\n'
                f'完成后将执行报告写入 {rpt}。')
    
    return None

def on_done(result_text):
    """agentmain --reflect 调用的回调"""
    # 提取任务ID和报告路径
    tid_match = re.search(r'^\[定时任务\]\s*(.+)$', result_text, flags=re.M)
    rpt_match = re.search(r'^\[报告路径\]\s*(.+)$', result_text, flags=re.M)
    
    if not tid_match:
        _logger.warning(f'on_done: cannot extract task_id from result')
        return
    
    tid = tid_match.group(1).strip()
    rpt = rpt_match.group(1).strip() if rpt_match else None
    
    # 检查报告是否成功生成
    success = bool(rpt and os.path.isfile(rpt) and os.path.getsize(rpt) > 100)
    
    error_msg = ''
    if not success:
        if '错误' in result_text or 'ERROR' in result_text:
            error_msg = result_text[:500]
    
    # 读取任务配置
    task_file = os.path.join(TASKS, f'{tid}.json')
    try:
        with open(task_file, encoding='utf-8') as f:
            task_config = json.load(f)
    except:
        task_config = {}
    
    # 发布完成/失败事件
    event_type = 'task_completed' if success else 'task_failed'
    _publish_event(event_type, tid, task_config, result={
        'success': success,
        'report_path': rpt if success else None,
        'error': error_msg if not success else None
    }, metadata={
        'complete_time': datetime.now().isoformat()
    })
    
    _logger.info(f'{event_type.upper()} {tid} success={success} report={rpt}')
```

#### 修改2：强化端口锁

```python
# scheduler.py:6-15 (替换原有代码)
try: 
    _lock
    _logger.info(f'Scheduler port lock already held (hot reload, pid={os.getpid()})')
except NameError:
    try:
        _lock = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
        _lock.bind(('127.0.0.1', 45762))
        _lock.listen(1)
        _logger.info(f'Scheduler port lock acquired (pid={os.getpid()})')
    except OSError as e:
        _logger.error(f'FATAL: Another scheduler instance is running (port 45762 busy)')
        _logger.error(f'Check with: lsof -i:45762 (Linux/Mac) or netstat -ano | findstr 45762 (Windows)')
        raise SystemExit(1) from e
```

---

### 4.2 修改微信Bot订阅实现（P0）

#### 删除内置调度器

**文件**: `frontends/wechatapp.py`

**删除以下函数**（426-446行）：
- `_load_scheduler_module()`
- `start_scheduler()`

#### 实现订阅者

**新增**：

```python
import os, sys, re, threading, queue, time, socket, json, struct, base64, uuid, hashlib, math
from pathlib import Path
from urllib.parse import quote
import requests, qrcode
from Crypto.Cipher import AES

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
_ROOT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
_TEMP_DIR = os.path.join(_ROOT_DIR, 'temp')
_STATE_FILE = os.path.join(_TEMP_DIR, 'wechatapp_state.json')
from agentmain import GeneraticAgent
from llmcore import reload_mykeys

# ... 保留你的状态管理函数 ...
# _load_state(), _save_state(), _remember_user(), _scheduler_target(), _send_text_chunks()

def _read_file_if_exists(path):
    """读取文件（如果存在）"""
    if not path or not os.path.isfile(path):
        return ''
    try:
        with open(path, encoding='utf-8', errors='replace') as f:
            return f.read()
    except Exception as e:
        print(f'[WX] read file error: {e}', file=sys.__stdout__)
        return ''

class WeChatTaskSubscriber:
    """微信Bot任务通知订阅者"""
    
    def __init__(self, bot):
        self.bot = bot
        self.event_file = os.path.join(_TEMP_DIR, 'scheduler_notifications/events.jsonl')
        self.offset_file = os.path.join(_TEMP_DIR, '.wechat_subscriber_offset')
        self._load_offset()
    
    def _load_offset(self):
        try:
            with open(self.offset_file) as f:
                self.offset = int(f.read().strip())
        except:
            self.offset = 0
    
    def _save_offset(self):
        with open(self.offset_file, 'w') as f:
            f.write(str(self.offset))
    
    def poll_events(self):
        """轮询新事件"""
        if not os.path.exists(self.event_file):
            return []
        
        events = []
        try:
            with open(self.event_file, encoding='utf-8') as f:
                f.seek(self.offset)
                for line in f:
                    try:
                        event = json.loads(line.strip())
                        if self.should_handle(event):
                            events.append(event)
                    except json.JSONDecodeError:
                        pass
                self.offset = f.tell()
        except Exception as e:
            print(f'[WX Subscriber] poll error: {e}', file=sys.__stdout__)
        
        self._save_offset()
        return events
    
    def should_handle(self, event):
        """只处理完成和失败事件"""
        return event.get('event_type') in ('task_completed', 'task_failed')
    
    def handle_event(self, event):
        """处理事件：推送到微信"""
        # 获取推送目标
        uid, ctx = _scheduler_target()
        if not uid:
            print(f'[WX Subscriber] no push target for {event["task_id"]}', 
                  file=sys.__stdout__)
            return
        
        tid = event.get('task_id', 'unknown')
        event_type = event.get('event_type')
        result = event.get('result', {})
        
        if event_type == 'task_completed':
            # 成功：读取报告内容
            report_path = result.get('report_path', '')
            report = _read_file_if_exists(report_path)
            
            if report.strip():
                msg = f'✅ [定时任务] {tid}\n[状态] 完成\n\n{report}'
            else:
                msg = f'⚠️ [定时任务] {tid}\n[状态] 完成但报告为空\n[路径] {report_path}'
        else:
            # 失败
            error = result.get('error', '(无错误信息)')
            msg = f'❌ [定时任务] {tid}\n[状态] 失败\n\n{error}'
        
        # 推送
        try:
            _send_text_chunks(self.bot, uid, msg, context_token=ctx)
            print(f'[WX Subscriber] pushed {tid} to {uid}', file=sys.__stdout__)
        except Exception as e:
            print(f'[WX Subscriber] push failed: {type(e).__name__}: {e}', 
                  file=sys.__stdout__)
    
    def start(self):
        """启动订阅（后台线程）"""
        def _loop():
            print('[WX Subscriber] started', file=sys.__stdout__)
            while True:
                try:
                    events = self.poll_events()
                    for event in events:
                        try:
                            self.handle_event(event)
                        except Exception as e:
                            print(f'[WX Subscriber] handle error: {e}', file=sys.__stdout__)
                    time.sleep(10)  # 每10秒检查一次
                except Exception as e:
                    print(f'[WX Subscriber] loop error: {e}', file=sys.__stdout__)
                    time.sleep(30)
        
        threading.Thread(target=_loop, daemon=True).start()

# ... 保留你的 on_message 函数 ...

if __name__ == '__main__':
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument('--relogin', action='store_true')
    parser.add_argument('--llm_no', type=int)
    args, _unknown = parser.parse_known_args()
    
    try: 
        _lock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        _lock.bind(('127.0.0.1', 19531))
    except OSError: 
        print('[WeChat] Another instance running, exiting.')
        sys.exit(1)
    
    _logf = open(os.path.join(_ROOT_DIR, 'temp', 'wechatapp.log'), 
                 'a', encoding='utf-8', buffering=1)
    sys.stdout = sys.stderr = _logf
    print(f'[NEW] Process starting {time.strftime("%m-%d %H:%M")}')
    
    bot = WxBotClient()
    if args.relogin or not bot.token:
        if not sys.stdout.isatty():
            print('[Bot] no token and not interactive, exit.')
            sys.exit(1)
        sys.stdout = sys.stderr = sys.__stdout__
        bot.login_qr()
        sys.stdout = sys.stderr = _logf
    
    if args.llm_no is not None:
        try:
            agent.next_llm(args.llm_no)
            print(f'[WX] llm_no={agent.llm_no} {agent.get_llm_name()}')
        except Exception as e:
            print(f'[WX] set llm_no failed: {e}')
    
    threading.Thread(target=agent.run, daemon=True).start()
    
    # 启动任务通知订阅（新增）
    subscriber = WeChatTaskSubscriber(bot)
    subscriber.start()
    
    print(f'WeChat Bot 已启动 (bot_id={bot.bot_id})', file=sys.__stdout__)
    try:
        bot.run_loop(on_message)
    except AuthExpired:
        print('[Bot] token expired, exit.', file=sys.__stdout__)
        sys.exit(2)
```

---

### 4.3 更新launch.pyw（P0）

**文件**: `launch.pyw`

```python
# launch.pyw:117-148 (修改)

if __name__ == '__main__':
    # ... 现有的参数解析和其他Bot启动 ...
    
    # === 微信Bot ===
    if args.wechat:
        wxcmd = [sys.executable, os.path.join(frontends_dir, 'wechatapp.py')]
        if args.llm_no:
            wxcmd += ['--llm_no', str(args.llm_no)]
        wxproc = subprocess.Popen(wxcmd, 
                                  creationflags=subprocess.CREATE_NO_WINDOW if os.name=='nt' else 0)
        atexit.register(wxproc.kill)
        print('[Launch] WeChat Bot started (subscribes to scheduler notifications)')
    else: 
        print('[Launch] WeChat Bot not enabled (use --wechat to start)')
    
    # === 定时任务调度器 ===
    # 注意：调度器现在总是独立启动，不再与微信Bot耦合
    if args.sched:
        scheduler_proc = subprocess.Popen(
            [sys.executable, os.path.join(script_dir, "agentmain.py"), 
             "--reflect", os.path.join(script_dir, "reflect", "scheduler.py"), 
             "--llm_no", str(args.llm_no)], 
            creationflags=subprocess.CREATE_NO_WINDOW if os.name=='nt' else 0)
        atexit.register(scheduler_proc.kill)
        print('[Launch] Task Scheduler started (independent, publishes notifications)')
    else: 
        print('[Launch] Task Scheduler not enabled (--sched)')
```

---

## 五、推送集成指南

### 5.1 为其他Bot添加订阅

以**飞书Bot**为例（`frontends/feishuapp.py`）：

```python
import os, sys, json, threading, time
# ... 飞书SDK导入 ...

_TEMP_DIR = os.path.join(os.path.dirname(os.path.dirname(__file__)), 'temp')

class FeishuTaskSubscriber:
    """飞书Bot任务通知订阅者"""
    
    def __init__(self, app):
        self.app = app  # 飞书App实例
        self.event_file = os.path.join(_TEMP_DIR, 'scheduler_notifications/events.jsonl')
        self.offset_file = os.path.join(_TEMP_DIR, '.feishu_subscriber_offset')
        self._load_offset()
    
    def _load_offset(self):
        try:
            with open(self.offset_file) as f:
                self.offset = int(f.read().strip())
        except:
            self.offset = 0
    
    def _save_offset(self):
        with open(self.offset_file, 'w') as f:
            f.write(str(self.offset))
    
    def poll_events(self):
        """轮询新事件"""
        if not os.path.exists(self.event_file):
            return []
        
        events = []
        with open(self.event_file, encoding='utf-8') as f:
            f.seek(self.offset)
            for line in f:
                try:
                    event = json.loads(line.strip())
                    if self.should_handle(event):
                        events.append(event)
                except json.JSONDecodeError:
                    pass
            self.offset = f.tell()
        
        self._save_offset()
        return events
    
    def should_handle(self, event):
        """只处理完成事件"""
        return event.get('event_type') == 'task_completed'
    
    def handle_event(self, event):
        """处理事件：推送到飞书"""
        tid = event['task_id']
        result = event.get('result', {})
        report_path = result.get('report_path', '')
        
        # 读取报告
        if report_path and os.path.isfile(report_path):
            with open(report_path, encoding='utf-8') as f:
                report = f.read()
        else:
            report = '(报告文件不存在)'
        
        # 构建飞书消息（Markdown格式）
        msg = {
            "msg_type": "interactive",
            "card": {
                "header": {"title": {"tag": "plain_text", "content": f"✅ 定时任务: {tid}"}},
                "elements": [
                    {"tag": "markdown", "content": report}
                ]
            }
        }
        
        # 获取推送目标（从配置或状态读取）
        target_chat_id = self._get_target_chat()
        if not target_chat_id:
            print(f'[Feishu Subscriber] no target chat for {tid}')
            return
        
        # 推送
        try:
            self.app.send_message(target_chat_id, msg)
            print(f'[Feishu Subscriber] pushed {tid} to {target_chat_id}')
        except Exception as e:
            print(f'[Feishu Subscriber] push failed: {e}')
    
    def _get_target_chat(self):
        """获取推送目标群聊ID（从配置或状态）"""
        try:
            cfg = reload_mykeys()[0]
            return cfg.get('feishu_scheduler_chat_id', '')
        except:
            return ''
    
    def start(self):
        """启动订阅"""
        def _loop():
            print('[Feishu Subscriber] started')
            while True:
                try:
                    events = self.poll_events()
                    for event in events:
                        try:
                            self.handle_event(event)
                        except Exception as e:
                            print(f'[Feishu Subscriber] handle error: {e}')
                    time.sleep(10)
                except Exception as e:
                    print(f'[Feishu Subscriber] loop error: {e}')
                    time.sleep(30)
        
        threading.Thread(target=_loop, daemon=True).start()

# 在 feishuapp.py 的 __main__ 中：
if __name__ == '__main__':
    # ... 飞书Bot初始化 ...
    
    # 启动任务通知订阅
    subscriber = FeishuTaskSubscriber(app)
    subscriber.start()
    
    # ... 飞书Bot主循环 ...
```

### 5.2 订阅者模板

创建 `frontends/task_subscriber_base.py`（可选，用于复用）：

```python
import os, json, threading, time

class TaskSubscriberBase:
    """任务订阅者基类（模板）"""
    
    def __init__(self, bot_name):
        self.bot_name = bot_name
        _temp_dir = os.path.join(os.path.dirname(os.path.dirname(__file__)), 'temp')
        self.event_file = os.path.join(_temp_dir, 'scheduler_notifications/events.jsonl')
        self.offset_file = os.path.join(_temp_dir, f'.{bot_name}_subscriber_offset')
        self._load_offset()
    
    def _load_offset(self):
        try:
            with open(self.offset_file) as f:
                self.offset = int(f.read().strip())
        except:
            self.offset = 0
    
    def _save_offset(self):
        with open(self.offset_file, 'w') as f:
            f.write(str(self.offset))
    
    def poll_events(self):
        if not os.path.exists(self.event_file):
            return []
        
        events = []
        with open(self.event_file, encoding='utf-8') as f:
            f.seek(self.offset)
            for line in f:
                try:
                    event = json.loads(line.strip())
                    if self.should_handle(event):
                        events.append(event)
                except json.JSONDecodeError:
                    pass
            self.offset = f.tell()
        
        self._save_offset()
        return events
    
    def should_handle(self, event):
        """过滤事件（子类可覆盖）"""
        return event.get('event_type') in ('task_completed', 'task_failed')
    
    def handle_event(self, event):
        """处理事件（子类必须实现）"""
        raise NotImplementedError(f'{self.__class__.__name__} must implement handle_event()')
    
    def start(self):
        def _loop():
            print(f'[{self.bot_name} Subscriber] started')
            while True:
                try:
                    events = self.poll_events()
                    for event in events:
                        try:
                            self.handle_event(event)
                        except Exception as e:
                            print(f'[{self.bot_name} Subscriber] handle error: {e}')
                    time.sleep(10)
                except Exception as e:
                    print(f'[{self.bot_name} Subscriber] loop error: {e}')
                    time.sleep(30)
        
        threading.Thread(target=_loop, daemon=True).start()
```

**使用示例**（钉钉Bot）：

```python
from task_subscriber_base import TaskSubscriberBase

class DingTalkTaskSubscriber(TaskSubscriberBase):
    def __init__(self, bot):
        super().__init__('dingtalk')
        self.bot = bot
    
    def handle_event(self, event):
        # 钉钉特定的推送逻辑
        tid = event['task_id']
        # ... 推送到钉钉群 ...
```

---

## 六、实施路线图

### 6.1 阶段划分

| 阶段 | 任务 | 优先级 | 预计时间 | 依赖 |
|------|------|--------|---------|------|
| **Phase 1** | 修复调度器核心bug | P0 🔴 | 2小时 | - |
| **Phase 2** | 实现通用通知层 | P0 🔴 | 1小时 | Phase 1 |
| **Phase 3** | 改造微信Bot订阅 | P0 🔴 | 1.5小时 | Phase 2 |
| **Phase 4** | 测试验证 | P0 🔴 | 1小时 | Phase 3 |
| **Phase 5** | 其他Bot集成（可选） | P1 🟡 | 每个Bot 30分钟 | Phase 4 |
| **Phase 6** | 添加任务创建工具（可选） | P2 🟢 | 1小时 | - |

### 6.2 实施步骤

#### Phase 1: 修复调度器核心bug（P0，2小时）

**步骤**：

1. **备份当前代码**
   ```bash
   cp reflect/scheduler.py reflect/scheduler.py.backup
   ```

2. **修改 `reflect/scheduler.py`**
   - 添加 `_load_attempts()`, `_save_attempt()`, `_cleanup_old_attempts()`
   - 修改 `check()` 函数，使用持久化的 `attempt_cache`
   - 强化端口锁（捕获异常并退出）
   - 添加 `_publish_event()` 和 `on_done()` 函数

3. **测试**
   ```bash
   # 删除旧状态
   rm -f sche_tasks/.last_attempt.json
   rm -rf sche_tasks/done/*
   
   # 启动调度器
   python agentmain.py --reflect reflect/scheduler.py
   
   # 观察日志30分钟
   tail -f sche_tasks/scheduler.log
   
   # 验证：任务应该只触发1次
   # 验证：.last_attempt.json 文件被创建
   cat sche_tasks/.last_attempt.json
   ```

4. **热重载测试**
   ```bash
   # 修改 scheduler.py（添加注释）
   echo "# test hot reload" >> reflect/scheduler.py
   
   # 观察日志：应显示 [Reflect] reloaded
   # 验证：.last_attempt.json 仍存在且未清空
   ```

5. **双实例测试**
   ```bash
   # 启动第一个调度器
   python agentmain.py --reflect reflect/scheduler.py &
   
   # 尝试启动第二个（应该立即退出）
   python agentmain.py --reflect reflect/scheduler.py
   # 预期输出：FATAL: Another scheduler instance is running
   ```

---

#### Phase 2: 实现通用通知层（P0，1小时）

**步骤**：

1. **验证通知文件生成**
   - 启动调度器
   - 等待任务触发
   - 检查 `temp/scheduler_notifications/events.jsonl`
   
   ```bash
   cat temp/scheduler_notifications/events.jsonl
   # 预期：每个任务触发/完成都有对应事件
   ```

2. **验证事件格式**
   ```bash
   # 解析第一行JSON
   head -1 temp/scheduler_notifications/events.jsonl | jq .
   
   # 预期输出：
   # {
   #   "event_id": "...",
   #   "timestamp": "2026-06-11T...",
   #   "event_type": "task_triggered",
   #   "task_id": "daily_news_summary",
   #   ...
   # }
   ```

---

#### Phase 3: 改造微信Bot订阅（P0，1.5小时）

**步骤**：

1. **修改 `frontends/wechatapp.py`**
   - 删除 `start_scheduler()` 函数
   - 添加 `WeChatTaskSubscriber` 类
   - 在 `__main__` 中启动订阅

2. **修改 `launch.pyw`**
   - 移除微信Bot的 `--sched` 参数传递
   - 确保调度器独立启动

3. **测试订阅**
   ```bash
   # 终端1：启动调度器
   python agentmain.py --reflect reflect/scheduler.py
   
   # 终端2：启动微信Bot
   python frontends/wechatapp.py
   
   # 终端3：观察微信Bot日志
   tail -f temp/wechatapp.log | grep Subscriber
   
   # 预期输出：
   # [WX Subscriber] started
   # [WX Subscriber] pushed daily_news_summary to wxid_xxx
   ```

4. **验证offset机制**
   ```bash
   # 检查offset文件
   cat temp/.wechat_subscriber_offset
   # 应该是一个数字（文件读取位置）
   
   # 重启微信Bot，验证不会重复推送
   ```

---

#### Phase 4: 综合测试（P0，1小时）

**测试用例**：

1. **正常流程**
   ```bash
   # 1. 清理环境
   rm -f sche_tasks/.last_attempt.json
   rm -rf sche_tasks/done/*
   rm -f temp/scheduler_notifications/events.jsonl
   rm -f temp/.wechat_subscriber_offset
   
   # 2. 创建测试任务（2分钟后触发）
   cat > sche_tasks/test_task.json <<EOF
   {
     "schedule": "$(date -d '+2 minutes' +%H:%M)",
     "repeat": "once",
     "enabled": true,
     "prompt": "输出当前时间和系统信息"
   }
   EOF
   
   # 3. 启动调度器 + 微信Bot
   python launch.pyw --sched --wechat
   
   # 4. 等待5分钟，观察：
   #    - 调度器日志：任务触发1次
   #    - 通知文件：生成task_triggered和task_completed事件
   #    - 微信Bot日志：成功推送
   #    - 微信：收到消息
   ```

2. **失败重试**
   ```bash
   # 创建必定失败的任务
   cat > sche_tasks/fail_task.json <<EOF
   {
     "schedule": "$(date -d '+1 minute' +%H:%M)",
     "repeat": "once",
     "enabled": true,
     "retry_cooldown_minutes": 5,
     "prompt": "故意调用不存在的工具 nonexistent_tool()"
   }
   EOF
   
   # 观察：
   # - 第1分钟：触发
   # - 第3分钟：不应重复触发（retry_cooldown=5分钟）
   # - 微信收到失败通知
   ```

3. **多Bot并行**
   ```bash
   # 同时启动微信Bot + 飞书Bot（如果已实现）
   python launch.pyw --sched --wechat --feishu
   
   # 验证：两个Bot都收到推送
   ```

---

#### Phase 5: 其他Bot集成（可选，P1）

为每个Bot（飞书、钉钉、企业微信）：

1. 创建订阅者类（参照 `WeChatTaskSubscriber`）
2. 在Bot主文件中启动订阅
3. 测试推送

---

#### Phase 6: 添加任务创建工具（可选，P2）

参照之前文档的 `schedule_task()` 实现。

---

### 6.3 验收标准

| 项目 | 验收标准 | 测试方法 |
|------|---------|---------|
| **无重复触发** | 任务在done目录为空时也只触发1次 | 观察日志30分钟 |
| **热重载安全** | scheduler.py热重载后不重复触发 | 修改文件触发重载 |
| **端口锁生效** | 第二个调度器实例立即退出 | 尝试启动双实例 |
| **持久化有效** | 重启后不立即重复触发 | kill调度器后重启 |
| **通知生成** | 每个任务都有对应事件 | 检查events.jsonl |
| **微信推送** | 微信收到完整报告 | 实际触发任务 |
| **offset机制** | 重启Bot不重复推送 | 重启微信Bot |
| **多Bot支持** | 飞书/钉钉同时收到推送 | 多Bot并行测试 |

---

## 七、文档与配置

### 7.1 配置示例

#### mykey.py

```python
# === 定时任务推送配置 ===

# 微信Bot推送目标
wechat_scheduler_user_id = 'wxid_xxxxxxxxx'  # 微信用户ID（可选，也可用 /sched_target 命令设置）
wechat_scheduler_context_token = ''           # 上下文token（可选）

# 飞书Bot推送目标
feishu_scheduler_chat_id = 'oc_xxxxxxxxx'     # 飞书群聊ID

# 钉钉Bot推送目标
dingtalk_scheduler_webhook = 'https://oapi.dingtalk.com/robot/send?access_token=xxx'

# 企业微信Bot推送目标
wecom_scheduler_webhook = 'https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx'
```

### 7.2 目录结构

```
GenericAgent/
├── sche_tasks/
│   ├── daily_news_summary.json       # 任务定义
│   ├── .last_attempt.json            # 尝试记录（新增）
│   └── done/
│       └── 2026-06-11_1030_xxx.md    # 报告
├── temp/
│   ├── scheduler_notifications/
│   │   └── events.jsonl              # 通知队列（新增）
│   ├── .wechat_subscriber_offset     # 微信Bot读取位置（新增）
│   ├── .feishu_subscriber_offset     # 飞书Bot读取位置（新增）
│   └── wechatapp_state.json          # 微信Bot状态
├── frontends/
│   ├── wechatapp.py                  # 微信Bot（修改）
│   ├── feishuapp.py                  # 飞书Bot（需添加订阅）
│   ├── dingtalkapp.py                # 钉钉Bot（需添加订阅）
│   ├── wecomapp.py                   # 企业微信Bot（需添加订阅）
│   └── task_subscriber_base.py       # 订阅者基类（可选）
└── reflect/
    └── scheduler.py                  # 调度器（修改）
```

### 7.3 日志位置

| 日志文件 | 用途 | 清理策略 |
|---------|------|---------|
| `sche_tasks/scheduler.log` | 调度器触发/跳过/通知发布 | 每月归档 |
| `temp/reflect_logs/scheduler_*.log` | 任务执行结果 | 保留30天 |
| `temp/scheduler_notifications/events.jsonl` | 通知事件队列 | 每周轮转（保留最近1周） |
| `temp/wechatapp.log` | 微信Bot订阅和推送日志 | 每天轮转 |
| `sche_tasks/.last_attempt.json` | 尝试记录缓存 | 自动清理7天外 |

---

## 八、FAQ

**Q1: 为什么不在微信Bot内直接运行调度器？**

A: **职责分离** + **可靠性**。调度器是核心服务，应独立运行；Bot可能因网络、登录等问题崩溃，不应影响调度器。

---

**Q2: 通知文件会无限增长吗？**

A: 建议定期轮转。可添加cron任务：
```bash
# 每周日凌晨3点归档
0 3 * * 0 mv temp/scheduler_notifications/events.jsonl temp/scheduler_notifications/events_$(date +\%Y-\%m-\%d).jsonl && touch temp/scheduler_notifications/events.jsonl
```

---

**Q3: 如何确认某个Bot收到了推送？**

A: 检查Bot的日志：
```bash
# 微信
grep "pushed" temp/wechatapp.log

# 飞书
grep "pushed" temp/feishuapp.log
```

---

**Q4: 可以同时推送到多个微信用户吗？**

A: 可以。修改 `WeChatTaskSubscriber.handle_event()` 中的推送逻辑：
```python
# 从配置读取多个用户ID
target_users = cfg.get('wechat_scheduler_user_ids', [])  # 列表
for uid in target_users:
    _send_text_chunks(self.bot, uid, msg, ...)
```

---

**Q5: 如何过滤特定任务？**

A: 在订阅者的 `should_handle()` 中添加过滤：
```python
def should_handle(self, event):
    # 只处理daily_news_summary任务
    return (event.get('event_type') == 'task_completed' and 
            event.get('task_id') == 'daily_news_summary')
```

---

**Q6: 调度器崩溃后如何恢复？**

A: 
1. `.last_attempt.json` 持久化了尝试记录，重启后不会重复触发
2. `events.jsonl` 持久化了通知，Bot会从上次offset继续读取
3. 但**正在执行的任务会丢失**，建议配置进程监控（如systemd、supervisor）

---

## 九、总结

### 9.1 核心改进

✅ **修复调度器bug**
- 持久化防重机制（.last_attempt.json）
- 强化端口锁（第二个实例立即退出）
- 消除重复触发漏洞

✅ **通用推送架构**
- 调度器与Bot解耦
- 标准化事件格式
- 支持多Bot并行订阅

✅ **易扩展**
- 新增Bot只需实现订阅者接口
- 订阅者模板可复用
- 通知层无需修改

### 9.2 架构对比

| 维度 | 修改前 | 修改后 |
|------|-------|-------|
| **调度器** | 可能多实例冲突 | 唯一实例（端口锁） |
| **防重机制** | 内存字典（易丢失） | 持久化JSON（可靠） |
| **推送方式** | 微信Bot内置调度器 | 通用通知层 + 订阅 |
| **扩展性** | 每个Bot需独立实现 | 复用订阅者模板 |
| **可靠性** | Bot崩溃影响调度 | 调度器独立运行 |

### 9.3 下一步

1. **立即执行 Phase 1-4**（修复核心bug + 微信Bot订阅）
2. **逐步集成其他Bot**（飞书、钉钉、企业微信）
3. **考虑添加任务创建工具**（提升用户体验）
4. **配置监控告警**（调度器健康检查）

---

**文档状态**: ✅ Ready for Implementation  
**预计总工时**: 6-8小时（包含测试）
